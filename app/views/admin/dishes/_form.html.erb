<%= form_for(admin_dish, :html => { :multipart => true }) do |f| %>
  <% if admin_dish.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin_dish.errors.count, "error") %> prohibited this admin_dish from being saved:</h2>

      <ul>
      <% admin_dish.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :dishgroup %>
    <%= f.text_field :dishgroup %>
  </div>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description %>
  </div>

  <div class="field">
    <%= f.label :price %>
    <%= f.text_field :price %>
  </div>
  <div class="field">
    <%= f.label :photo %>
    <%= f.file_field :photo %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
