<%= form_for(admin_restaurant) do |f| %>
  <% if admin_restaurant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin_restaurant.errors.count, "error") %> prohibited this admin_restaurant from being saved:</h2>

      <ul>
      <% admin_restaurant.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description %>
  </div>

  <div class="field">
    <%= f.label :address %>
    <%= f.text_field :address %>
  </div>

  <div class="field">
    <%= f.label :phone %>
    <%= f.text_field :phone %>
  </div>

  <div class="field">
    <%= f.label :logo %>
    <%= f.text_field :logo %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
